#ifndef MEMORYMANAGER_H
#define MEMORYMANAGER_H

enum memory{BESTFIT, NEXTFIT, WORSTFIT, FIRSTFIT, BADINPUT};

memory parseMemoryString(std::string input){
    if(input == ""){return ;}
    if(input == ""){return ;}
    if(input == ""){return ;}
    if(input == ""){return ;}
    else{return BADINPUT;}}
std::deque best_fit(std::deque input, PCB node){
    return input;}

#endif // MEMORYMANAGER_H
